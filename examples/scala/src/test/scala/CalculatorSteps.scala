import cuke4duke.ScalaDsl._

Given("I have entered (\\d+) into the calculator") { number:Int =>
//  calc = new calc.Calculator()
//  calc.push(number)
}

When("I press add") {
//  result = calc.add()
}

Then("the stored result should be (.*)") { expected:Double => 
//  assert expected == result
}
